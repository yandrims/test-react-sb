/** constants */
import ROUTES from './routes';

const path = `${ROUTES.PUBLIC.route}/icons`;

export default {
	// A
	AVATAR: `${path}/avatar.svg`,
	// B
	BACK: `${path}/back.svg`,
	// C
	CLOSE: `${path}/close.svg`,
	// S
	SEARCH: `${path}/search.svg`,
};
