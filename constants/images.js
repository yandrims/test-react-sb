/** constants */
import ROUTES from './routes';

const path = `${ROUTES.PUBLIC.route}/images`;

export default {
	// A
	// ...
	// L
	LOGO: `${path}/logo.png`,
	// N
	NO_IMAGE: `${path}/no-image.jpg`,
};
